version: '3.7'

services:
  web:
    build: .
    command: python /diaryspace/manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/diaryspace
    ports:
      - 8000:8000
    depends_on:
      - db
    links:
      - db:db
  db:
    image: postgres:11
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    ports:
      - 5432:5432

volumes:
  postgres_data: